#!/usr/bin/env bash
# Starts the dbService daemon. Run this once and leave it running; use ./dbgit from another
# terminal (in the same working directory) to send it commands.
set -euo pipefail
cd "$(dirname "$0")"
exec mvn -q exec:java -Dexec.mainClass=org.example.dbgit.DbServiceMain
